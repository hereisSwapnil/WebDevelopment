<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= data["name"] %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <!-- Inlcuding ejs templtes -->
    <%- include("includes/header.ejs") %>
    <h2>Name: <%= data["name"] %>
            <h2>Followers: <%= data["followers"] %>
                    <h2>Following: <%= data["following"] %>
                    </h2>
                    <div class="grid grid-cols-1 gap-4 lg:grid-cols-4 lg:gap-8">
                        <% for( let index=0; index < data.posts.length; index++ ) { %>
                            <div class="h-32  rounded-lg bg-gray-200"><img src="<%= data.posts[index].image %>" alt="">
                                <p>likes: <%= data.posts[index].likes %>
                                </p>
                                <p>comments: <%= data.posts[index].comments %>
                                </p>
                            </div>
                            <% } %>
                    </div>
</body>

</html>