{
  "name": "mongoose-tut",
  "version": "1.0.0",
  "description": "- `sudo systemctl start mongod` - `sudo systemctl status mongod` - `sudo systemctl stop mongod` *when reqired to stop - `mongosh`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^7.6.3"
  }
}
